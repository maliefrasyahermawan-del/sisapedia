import 'package:flutter/material.dart';

/// Colors specific to the Pengolah role UI, matching the blue accent used
/// for "Pengolah" in the design reference (kept separate from
/// [AppColors] which is tuned for the Sumber role's green palette).
class PengolahColors {
  PengolahColors._();

  static const Color primary = Color(0xFF3B82F6);
  static const Color primaryDark = Color(0xFF1D4ED8);
  static const Color primarySoft = Color(0xFFEAF2FE);
}
